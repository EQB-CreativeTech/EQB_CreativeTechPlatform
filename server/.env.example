PORT=3847
CORS_ORIGINS=http://localhost:8080,http://127.0.0.1:8080

# Production (Render): set SUPABASE_* so subscribers survive redeploys.
# Run server/supabase/newsletter_subscribers.sql in Supabase SQL Editor first.
SUPABASE_URL=
SUPABASE_SERVICE_ROLE_KEY=

# Gmail example: use an App Password (not your normal password).
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=your@gmail.com
SMTP_PASS=your-app-password
MAIL_FROM="EQB Creative Tech <your@gmail.com>"
